# CPU-only PyTorch: the default Linux wheel on PyPI bundles multi-gigabyte CUDA
# libraries, which is the usual reason a free-tier build times out. This extra
# index publishes +cpu builds and pip prefers them (a local version sorts above
# the plain one), so a CPU-only local install is unaffected.
--extra-index-url https://download.pytorch.org/whl/cpu

numpy>=1.19.0
pandas>=1.1.0
torch>=1.9.0
scikit-learn>=0.24.0
requests>=2.26.0
joblib>=1.0.0
streamlit>=1.28.0
plotly>=5.0.0
matplotlib>=3.3.0
scipy>=1.5.0
urllib3>=1.26.0
python-dotenv>=0.19.0
streamlit-autorefresh>=1.0.1
